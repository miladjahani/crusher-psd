# Only this bot's own Telegram token goes here.
# End-users' Cloudflare tokens are NEVER stored in .env or anywhere on disk —
# they're typed into the chat at deploy time and discarded right after use.
BOT_TOKEN=123456:ABC-your-telegram-bot-token
