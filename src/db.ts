
---


طراحی: دو store. `scans` (متا + thumb + **جدول ذرات** + آمار + کالیبراسیون) و `images` (Blob تصویر، جدا). نتیجه: لیست تاریخچه فقط `scans` را می‌خواند (سریع، بدون لود مگابایت تصویر)؛ تصویر کامل فقط وقتی یک رکورد را باز می‌کنی لود می‌شود. Schema **versioned** است تا بعداً فیلدهای SCADA بدون پاک شدن داده اضافه شوند.
